enabled: []
disabled: []
severity_overrides:
  # Team policy: an unregistered tool/model/machine must block the merge,
  # not just warn (promotes the L4 registry gate from advisory to hard).
  validator_agent_registry: P0
